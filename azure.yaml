# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: az-auto-ai_v0.0.1
metadata:
  template: az-auto-ai_0.0.1

hooks:
    postprovision:
      posix:
        shell: sh
        run: azd env get-values > .env;
        interactive: true
        continueOnError: false
      windows:
        shell: pwsh
        run: $output = azd env get-values; Add-Content -Path .env -Value $output;
        interactive: true
        continueOnError: false

pipeline:
    variables:
        - AZURE_OPENAI_RESOURCE
        - AZURE_OPENAI_RESOURCE_GROUP
        - AZURE_OPENAI_RESOURCE_GROUP_LOCATION
        - AZURE_OPENAI_SKU_NAME
        - AZURE_OPENAI_API_VERSION
        - CREATE_ROLE_FOR_USER
        - AZURE_SPEECH_VOICE
        - AZURE_SPEECH_REGION
        - AZURE_SPEECH_KEY
        - GOOGLE_API_KEY
        - GOOGLE_CSE_ID
        - EMAIL_ADDRESS
        - GRAPH_CLIENT_ID
        - GRAPH_CLIENT_SECRET
        - AUTH_TENANT_ID
        - OPENAI_API_KEY
        - OPENAI_MODEL
        - TWILIO_ACCOUNT_SID
        - TWILIO_AUTH_TOKEN
        - TWILIO_FROM_PHONE_NUMBER
        - TEXT_TO_PHONE_NUMBER